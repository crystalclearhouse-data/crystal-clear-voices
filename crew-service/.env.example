# DiscoAgents Crew Service
# Copy to .env and fill in values

# LLM provider (crew.json uses openai by default)
OPENAI_API_KEY=sk-...

# Optional: override model at runtime
# CREWAI_MODEL=gpt-4.1-mini

# Service port (default 8000, matched by CREW_SERVICE_URL in voice-server/.env)
PORT=8000

# Workflow CLI

Quick CLI to list and inspect workflow metadata.

Usage:

```bash
# list all workflows
npm --prefix tools/workflow-cli run start -- list

# show a specific workflow
npm --prefix tools/workflow-cli run start -- show sophie-engagement-webhook
```
